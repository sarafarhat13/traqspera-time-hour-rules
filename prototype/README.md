# Traqspera Time Hour Rules (app)

Vite + React app for the Time Hour Rules prototype.

```bash
npm install
npm run dev
```

Uses Trimble Modus Web Components (`@trimble-oss/moduswebcomponents-react`).
